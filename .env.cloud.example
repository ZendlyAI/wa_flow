# Placeholder-only configuration. Keep real client credentials in secret storage.
PORT=8080

# Required only for live OAuth/Botpress flows; leave unset during validation.
# BOTPRESS_WEBHOOK_URL=<botpress-webhook-url>
# CLIENT_ID=<oauth-client-id>
# CLIENT_SECRET=<oauth-client-secret>
